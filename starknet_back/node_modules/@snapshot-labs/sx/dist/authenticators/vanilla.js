"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vanillaAuthenticator = {
    type: 'vanilla',
    createCall(envelope, selector, calldata) {
        const { space, authenticator } = envelope.data.message;
        return {
            contractAddress: authenticator,
            entrypoint: 'authenticate',
            calldata: [space, selector, calldata.length, ...calldata]
        };
    }
};
exports.default = vanillaAuthenticator;
